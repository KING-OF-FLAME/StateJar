# Backend origin (no trailing slash). Leave unset for local dev (Vite proxy handles /api).
#
# Vite bakes this in at BUILD time, not at runtime: changing it in the Vercel
# dashboard does nothing until you trigger a Redeploy.
VITE_API_URL=https://api.statejar.com
