fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pymysql>=1.1
cryptography>=42.0
pydantic>=2.7
pydantic[email]>=2.7
bcrypt>=3.2
pyjwt>=2.8
pydantic-settings>=2.3
python-dotenv>=1.0
slowapi>=0.1.9
pytest>=8.2
httpx>=0.27
tiktoken>=0.7

# ML extras: GLiNER extraction layer and semantic retrieval fallback
# CPU-only torch wheels — avoids pulling the multi-GB CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2,<3
gliner>=0.2.13
# all-MiniLM-L6-v2 embeddings for the retrieval fallback (~90MB model)
sentence-transformers>=3.0

